# Adapter runtime — see docker/clawcode-adapter/server.py.
# Versions are pinned to the same families used elsewhere in the stack
# (FastAPI/uvicorn ≥0.115, MCP SDK ≥1.2 — matches docker/searchbox/Dockerfile).
fastapi>=0.115,<1.0
uvicorn[standard]>=0.30,<1.0
pydantic>=2.5,<3.0
mcp>=1.2
pyyaml>=6.0
opentelemetry-api>=1.27
opentelemetry-sdk>=1.27
opentelemetry-exporter-otlp-proto-http>=1.27
# A2A bindings: httpx for the push-notification webhook dispatcher;
# grpcio[-tools] for the gRPC binding (Section 10). `grpcio-tools` is a
# build-time-only dep but kept here for `proto/regen.sh` to work from a
# fresh image too.
httpx>=0.27,<1.0
grpcio>=1.62,<2.0
grpcio-tools>=1.62,<2.0
# cryptography is used by the JWS-signing helper for /.well-known/agent-card.json
# (Section 8.4.2). Bound to a recent series to get ES256 + JWK serialization.
cryptography>=42,<45
# Canonical JSON (RFC 8785) for stable AgentCard signatures.
jcs>=0.2,<1.0
