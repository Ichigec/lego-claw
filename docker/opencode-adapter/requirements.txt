# Adapter runtime — see docker/opencode-adapter/server.py.
# Mirrors docker/clawcode-adapter/requirements.txt so the version drift
# stays minimal across the three adapters.
fastapi>=0.115,<1.0
uvicorn[standard]>=0.30,<1.0
pydantic>=2.5,<3.0
mcp>=1.2
pyyaml>=6.0
opentelemetry-api>=1.27
opentelemetry-sdk>=1.27
opentelemetry-exporter-otlp-proto-http>=1.27
# A2A bindings: httpx for push-notification webhooks, grpcio[-tools] for
# the gRPC binding (Section 10). `grpcio-tools` is build-time-only but
# kept in the image for `proto/regen.sh` from a freshly-built container.
httpx>=0.27,<1.0
grpcio>=1.62,<2.0
grpcio-tools>=1.62,<2.0
# cryptography is used by the JWS-signing helper for /.well-known/agent-card.json
# (Section 8.4.2).
cryptography>=42,<45
# Canonical JSON (RFC 8785) for stable AgentCard signatures.
jcs>=0.2,<1.0
