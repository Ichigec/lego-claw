# Identical surface as docker/clawcode-adapter/requirements.txt.
#
# Do not install the `openhands` CLI in this shared environment: the CLI pulls
# its own LiteLLM/OpenTelemetry/browser stack and conflicts with the adapter's
# tracing dependencies on aarch64. The Dockerfile installs it as an isolated
# `uv tool` instead.
fastapi>=0.115,<1.0
uvicorn[standard]>=0.30,<1.0
pydantic>=2.5,<3.0
mcp>=1.2
pyyaml>=6.0
opentelemetry-api>=1.27
opentelemetry-sdk>=1.27
opentelemetry-exporter-otlp-proto-http>=1.27
# A2A bindings (see docker/clawcode-adapter/requirements.txt for rationale).
httpx>=0.27,<1.0
grpcio>=1.62,<2.0
grpcio-tools>=1.62,<2.0
cryptography>=42,<45
jcs>=0.2,<1.0
